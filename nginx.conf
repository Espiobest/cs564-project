worker_processes 1;

events {
    worker_connections 1024;
}

stream {
    # Implant beacon port
    server {
        listen 9999;
        proxy_pass c2-server:9999;
        proxy_connect_timeout 10s;
        proxy_timeout         300s;
    }
}
